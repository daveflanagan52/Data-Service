{
  "name": "@dataservice/root",
  "version": "1.0.0",
  "license": "MIT",
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "**/mochawesome",
      "**/mochawesome/**"
    ]
  },
  "scripts": {
    "build": "lerna run build",
    "build:client": "lerna run build:client && docker-compose build client",
    "build:server": "lerna run build:server && docker-compose build server",
    "docker:build": "docker-compose build",
    "docker:build:db": "yarn docker:build db",
    "docker:up": "docker-compose up",
    "docker:up:db": "docker-compose up db",
    "docker:down": "docker-compose down"
  },
  "dependencies": {
    "lerna": "^4.0.0"
  }
}
